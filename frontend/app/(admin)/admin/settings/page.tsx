import { SettingsView } from "@/components/admin/settings/settings-view";

// Configuración → Asistente (base). Las otras secciones son rutas hermanas.
export default function SettingsAsistentePage() {
  return <SettingsView tab="asistente" />;
}
